{
  "_comment": "train.py --config 示例：vis 波段 L_path 网（PCA 初始化解码头）。命令行参数优先于本文件。",
  "width": 512,
  "blocks": 6,
  "pca_mode": "head",
  "optimizer": "adamw",
  "lr": 1e-3,
  "weight_decay": 1e-4,
  "batch_size": 1024,
  "epochs": 300,
  "patience": 30,
  "ema_decay": 0.999,
  "preload": true
}
